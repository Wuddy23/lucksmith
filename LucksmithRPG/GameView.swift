// This file provides the SKView subclass used as the root view in
// GameViewController. Add this class name to your storyboard's view,
// or set it programmatically — see README for setup instructions.

import SpriteKit

// SKView is already the right class; no subclass needed.
// GameViewController loads it directly via view as? SKView.
